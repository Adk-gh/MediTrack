/*
Detects clicks outside a given element – great for closing modals, pop‑overs, or dropdowns.

useClickOutside(ref, () => closeDropdown());

You can expose a small <ClickOutside> component that wraps any panel. */